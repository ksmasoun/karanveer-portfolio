export interface Personal { first: string, last: string;}
export interface Social { linkedin: string, gmail:string, github:string}
export interface Project {id: number;  title: string; subtitle: string;image: string; description: string; link: string;}